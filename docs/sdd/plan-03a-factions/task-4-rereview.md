# Task 4 scoped 复审记录（Fix round 1）· 6c35c26..3223509

> 复审者：只读 `reviewer`（deepseek-flash，run f33a7ea9）｜修复 diff：`review-6c35c26..3223509.diff`（1 commit / 12321B；控制器的计划文本提交 `6c35c26` 被排除）
> 结论：**Fix round 1 = Accepted**；I1 / M1 / M2 / M3 / M5 全部**已解决**；新引入 2 条 Minor（N1/N2）
> 控制器核对（`task-4-fix1-verify.log`）：`EXIT=0`、`[factions] 115 失败=0`、`SCRIPT ERROR` 2 条 = 基线

## Findings 判定

| Finding | 判定 | 关键证据（复审者给出） |
| --- | --- | --- |
| **I1** rivals 去重丢 5/11 敌对对 | **已解决** | `factions.gd:236,246-249`：`pair_key := id + "\|" + oid if id < oid else oid + "\|" + id` + `seen`（在等实力 `continue` **之前**置位）→ 两种迭代顺序都得到 `min\|max`，同对恰好处理一次。复审者**独立复算内容表**：11 对，旧实现处理 6 / 丢 5，与控制器点名 5 对及 `fix1-pairs.log` 逐字吻合 |
| **M1** 报告账目 | **已解决** | `30+2+5+5+1+5+3 = 51`；`60+51=111`；修复轮 `+2(I1)+1(M2)+2(M3)−1(M5) = +4 → 115`，+4 可由 diff 独立核对（删 2 加 5 净 +1 拆条） |
| **M2** 自指断言 | **已解决** | `tests:263-282` 真逐字段遍历 + `mismatches` 列表；`compared_fields == 102` 是**观测值**（17×6 键，与 `ensure_state` 建的 6 个键一致，非恒等式）；`JSON.stringify` 护栏保留 |
| **M3** `evolve()` 缺早退 | **已解决（生产代码）** | `factions.gd:270` 守卫；**风险 2 核对**：合法世界仍走 `initialize → 回归 → 压制 → :290 写 GOVERNMENT_FLAG → return []`，Task 5 的 `for ev in evolve(world)` 仍成立；全仓 `evolve` 仅 6 个测试调用点。断言判别力见 N1 |
| **M5** 种子余量 | **已解决** | 被删断言已在 diff 中消失；保留的 `near(power, SUPPRESS_FLOOR, 1e-4)` 经数值与反证日志**双向确认**：纯回归值 ∈ `[0.0556, 0.0956]`，最小距容差上界仍 0.0056（56×）⇒ 无压制时**对任何 seed 必红**；有压制时最坏噪音下累计压制 ≥0.058 > 所需 ≤0.0456 ⇒ 必被 clamp 到 0.05、**对任何 seed 必绿**。实测 `0.064318 = 0.0756 − 0.011282` 与手算逐位吻合 |
| M4 / M6 / M7 | **未被误改** | M4 的运行时守卫等于权威计划文本；未拆分 `factions.gd`；`validate_content` 未动 |

**风险 4（新回归用例在旧逻辑下是否必红）**：`tests:313-322`——旧逻辑下 `black_market` 完全不进 `seen`，恒 0.60 ⇒ 断言 1 必红；断言 2（`auror_office > 0.90`）在新旧实现下都绿（旧逻辑仍处理 `auror_office↔death_eaters`，= 0.91275），计划 `:996` 已声明「旧实现下它是唯一会红的断言」，与 `fix1-red-asym.log`（113/1，唯一红为断言 1）一致 ⇒ **不算缺陷**。

## 新引入的问题

### N1（Minor）两条 M3 断言无判别力
`tests/factions_test.gd:387-388` 两条断言**无法变红**：`evolve()` 返回类型是 `-> Array`，守卫被移除时调用因 `world.clock.turn` 报错中止，Godot 返回该类型默认值 `[]` ⇒ `evolve(naked).size() == 0` 在有无守卫时都成立。证据是 `fix1-red-m3.log`：计数到的失败是**无关的** I1 断言（那次同时回退了去重），两条 `SCRIPT ERROR: … 'turn' … at: evolve (factions.gd:258)` 只是 stderr 噪音。
**影响**：M3 的回归只能靠「`SCRIPT ERROR` 条数 = 基线 2」这一通道发现，断言本身不承重（也与仓库既有 `§8#56`「非中止运行期错误仍 EXIT=0」同源）。
**控制器裁定：接受并登记**（不修）—— 保留断言作为意图文档；M3 的真正看护是 `task-1-baseline.log` 的噪音基线对比（控制器每轮都在核）。附带报告叙述不实（§⑩.4 写「套件因此中止、未调用 report」，但日志确有 `断言=115 失败=1`）→ 记为该报告的表述瑕疵。

### N2（Minor）文档漂移
权威计划 Task 4 的 `evolve()` 代码块**没有** M3 的早退，而实现有 → Task 5 若照抄计划块会丢守卫。另：`tests/factions_test.gd:328-329` 注释仍写「4 对…≈0.09」（I1 修复前的口径，现为 6 对，报告 §⑩.5 又写 ≈0.06），数字互不一致（因 clamp 到下限，结论不受影响）。
**处置**：① 控制器**已改计划**（提交 `a1fa03d`：`evolve()` 块补早退 + 注明「Task 5 接线务必保留」）；② 测试注释digit 失真 → 并入 Task 5 dispatch 顺手改正一句话。

## ⚠️ 复审者无法判定（控制器已处理）
1. 红/绿日志不在包内 → 复审者按代码推理并与之交叉核对，**两处一致**（113/1、0.064318 逐位、115/0、11 对/5 丢）。
2. `asym` 夹具最终值（手算 `black_market=0.594227`、`auror_office=0.917441`）无日志打印 → 依赖手算 + 全场绿灯；位级确认非必需。
3. Task 3 顶端 60 / 首轮 111 的基数无法从 diff 复算；可核对的增量 +4 已核对。
