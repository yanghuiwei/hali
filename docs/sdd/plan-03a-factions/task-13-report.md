# Task 13 · Step 3（文档更新）报告

- 提交：`f992927 docs(03a): Task 13 —— 更新 README/HANDOFF/NEXT-STEPS（03a 与 03a-P 收尾）`
- 基线：`e6a3f28`｜**只改了这三个文件**（`git diff --cached --name-only` = README.md / HANDOFF.md / NEXT-STEPS.md）
- 仓库根 5 个 `.ps_*.txt` 是外部 agent 的临时文件：**未 add、未删**

## 门禁（本轮实跑）

```
bash tools/test.sh            → EXIT=0；21 套件 / 1985 断言 / 失败 0；4 步全过
                                SCRIPT ERROR = 2   ERROR = 7（噪音门禁通过）
提交后复跑 test.sh             → EXIT=0；21 套件 / 1985 断言（文档改动未影响绿灯）
```
（`b1_acceptance.sh` 151 断言 / 0 失败 由控制器本轮实测；本次未重复跑，因为它写 `user://` 且与文档无关。）

## A. README.md

- 顶部：计划 01/02/**03a（含 03a-P）**均已合入 `main`；「当前进度」段同步
- 「后续计划」→ **03b 经济骨架 / 03c 社会与法律** 起（+ 存档格式 v2）
- 新增两节：`### 计划 03a · 派系与政治骨架（已完成）`、`### 计划 03a-P · 表现层与素材接线（已完成）`
  （后者含四个模块 / 两张清单 / 「换素材 = 改一行 JSON，零代码」/ 已知缺口指引 §B6）
- `## 运行` 补：**21 套件 / 1985 断言**、**stderr 噪音门禁（2 与 7）**、b1 **151 断言**
- 目录约定：补 `data/presentation.json`、`data/audio_cues.json`、`assets/`、`src/ui` 表现层四件套
- 调试镜像覆盖：7 个下拉 → **8 个（含性别）**，补素材槽与音频切换两行
- 设计不变量：补「内容进 data（含表现层清单）+ 加素材零代码 + 缺素材必须优雅回退」
- ⚠️ 精确性修正：`audio_cues.json` **`cues` 现在是空的**（sfx 素材未入库）⇒ 措辞改为
  「时代/地点 BGM 映射 8 era + 21 location + `master_volume`；短音效 `cues` 暂为空 ⇒ 对应 cue 静音，代码侧 8 个触发点均已接好」

## B. HANDOFF.md（重点）

| 节 | 改动 |
| --- | --- |
| 头部 / §0 | 「最后更新」重写；**一句话状态 → 四个计划全部完成并入 `main`**（含 03a/03a-P 交付清单与素材线挂账 + CJK 硬事实） |
| §1 | 分支表改为「**交付分支 `main`** / **开发分支 `plan-03-factions`**」；新增**计划 03a·03a-P 历史段**（四段式）。**未钉死任何 HEAD/合入哈希**（明写「以 `git log` 为准」） |
| §2 | 期望输出换成实测 **21 套件 / 1985 断言**；**新增自动化 stderr 噪音门禁**说明（两条计数、`EXPECTED_*` 可覆盖、为什么必须有它）；b1 **105 → 151** |
| §3 | 目录补 `data/presentation.json` · `data/audio_cues.json` · `assets/` · `src/rules/factions` · `src/ui` 四件套 |
| §4 | 新增 **16–22** 共 7 条踩坑（见下） |
| §5.5（新节） | 「计划 03a / 03a-P 的关键结论」：规则层 6 条（机构控制权 / 权力四角占比语义 / 政体口径 / tension 配额 / 信息保护 / `stream_pick_weighted` + `_pick_by_roll`）+ 表现层 10 条（四 seam / 接口冻结 / 清单不进 Registry / 只登记存在的文件 / 零代码换素材 / 运行期主题不提交 `.tres` / 缺素材不占位 / BGM 循环在导入层 / `.import` 必须入库 / 噪音门禁） |
| §8 | 标闭合：`§8#7`(T7) · `§8#16`/`§8#21`(T12) · `§8#41`(T7) · `§8#55`(**复核：像素级与真实 OS 事件仍未做**) · `§8#58`/`§8#61`–`§8#65` · `§8#69`/`§8#70`(T12)；**新增 `§8#71`**（读档 `house_id` 归一化 → 存档 v2，含裁定理由与零成本缓解）与 **`§8#72`**（素材线三件 + 到场后两件零代码事）；补**编号命名空间说明**（`§8#N` 与 `§4 第 N 条` 各自独立，避免 grep 混淆） |
| §9 | 自检清单换成 21/1985/151；**新增一条**「`test.sh` 会同时校验两个 stderr 噪音计数」 |

### §4 新增的 7 条踩坑（16–22）

16. **只能靠外部 stderr 计数守住**（累计 5 例：§8#48/§8#56 + Task 4 N1 + Task 6 M3 + Task 10 I1 + 03a-P S2/D1，含实测数字）
17. **可空返回值不要用有类型接收**（运行期 `SCRIPT ERROR` + **中止所在函数**；解析期 vs 运行期两种不同）
18. **helper 层断言不足以保证「调用处传对参数」**（Task 12 D3a 实证：`"rarity"` 静默退化，只有端到端红）
19. **断言要「红得干净」**（可能为 null 的路径先判空/哨兵串，否则红的形态是套件中止并掩盖后续断言）
20. **清单里不许出现字面点号键**（`resolve()` 只嵌套下钻 ⇒ 字面键永取不到；已加响亮门禁）
21. **九宫格顺序冻结 `[上,右,下,左]`**（映射封在 `AssetSlots.patch_insets()` 唯一一处；**测试必须用非对称值**）
22. **不要用 PowerShell 驱动 git / 不许手改 `.git/`**（BOM 写坏 ref 的真实事故：worktree 引用悬空、`status` 把整仓库看成未跟踪）

## C. NEXT-STEPS.md

- **新增 §0A「工作模式：快跑模式」**：砍掉项 / 保留的 4 道门禁 / **为什么保留 90 秒 `test.sh`**（让红色可归因）/ 控制器的证据依据（真缺陷全是 1–20 行小修；真返工只有两类：不可逆的全局选择 + 接口扇出，后者靠「先冻结接口」规避）
- §0/§0.5 的基线与恢复指引数字全部换成实测；读文件顺序同步（03a 计划已全部完成、03a-P spec 已评审）
- 工作流段：改为「默认按 §0A 快跑模式」；**注明 `scripts/task-brief` / `scripts/review-package` 在本机不存在**（不要照抄旧命令）
- **B3/B4/B5 标完成**；**新增 B6**（素材合并 + 清单加行：含「只动 `assets/` 才合并」的校验、`--import` 后提交 `.import`、以及控制器要做的两件零代码事）与 **B7**（03b/03c，边界照 03a spec §14）
- §C 表：闭合行加 ✅ 并指向任务；新增 `§8#71`/`§8#72` 两行
- §D 第 2 条（每任务流程）改为快跑模式；§E 速查更新交付点与「下一步第一件事」

## 我判定「不确定而没有写」的项

1. **时序**：文档写的是**终态**（03a「13/13」+「已合入 `main`」），但本次提交发生在 `merge --no-ff` **之前**——
   合入由控制器在本提交之后立即执行。若合入失败或改期，§0/§1 与 README 的「已完成并入 main」措辞需要回退。
   （这是按控制器指令写的；此处明示，不做静默声明。）
2. **素材哈希**：HANDOFF §8#72 里的霞鹜文楷 sha256 我写成截断的 `39AD71…`——它来自外部 agent 的探针输出，
   **我未独立核对**（字号 25,575,676 B 同源）。合入素材分支时由控制器用 Godot `FontFile.has_char()` 复核覆盖。
3. **`bg_main.ogg` 时长异常**（211.88s vs 原 309.09s）**未写进本次三个文件**：控制器裁定由人类自行替换，属未定论，
   不宜在交接文档里写成结论；它仍在 spec §8.4 与台账里。
4. 未写「计划 03a 的 13 个任务各自断言数」等细粒度数据（避免与台账重复、且容易漂移）——一律指向 `docs/sdd/plan-03a-factions/progress.md`。

## 残余

- 三个文件均为手改，**未做外部链接可达性检查**（README/HANDOFF/NEXT-STEPS 里的相对链接沿用既有写法，只新增了
  同类路径）；如需可另跑一次链接检查。
- 本次未跑 `b1_acceptance.sh`（控制器已跑过：151 断言 / 0 失败）。
