# Task 3 独立审查记录（reviewer，只读，2026-09-18）

- 审查范围：提交 `281fdd6..50fc993`（`61d4ac1` docs(plan) + `50fc993` feat(rules)）
- 审查包：`review-281fdd6..50fc993.diff`
- 被审查工件：计划 Task 3、`task-3-brief.md`、`src/model/money.gd`、`src/rules/magic_level.gd`、`tests/money_test.gd`、`tests/magic_level_test.gd`、`tests/run_tests.gd`、正典 `哈利·波特·魔法纪元.md`
- 控制器提供证据：`bash tools/test.sh` 提交后 `[harness]8/0 [registry]26/0 [money]15/0 [magic_level]22/0`，总计失败=0，`ALL TESTS PASSED`，EXIT=0；提交前红态 `[money] 断言=15 失败=2`

## 结论

**Spec ✅ / Approved with findings** —— `Money` 与 `MagicLevel` 逐字实现计划（计划 771–798 / 811–844 与 892–966 两段代码块），两处被改动的测试期望与注释口径经正典复核为**唯一正确**，无夹带数值改动；唯一实质问题是流程门禁（提交时未自跑 Step 5、无 `task-3-report.md`）以及几条计划自带的测试强度缺口（P2）。

## 发现

### Critical
无。

### Important
1. **流程门禁未被执行（P1 流程，风险已由控制器补跑关闭，不阻塞合并）**
   - `.superpowers/sdd/.../` 下只有 `task-1-report.md`/`task-2-report.md`，**无 task-3-report.md**；计划 973–986 明确 Step 5「运行测试，确认通过」→ Step 6 才提交（`预期：[money] 失败=0、[magic_level] 失败=0`）。实现者既未写报告也未跑 Step 5。
   - `.git/logs/HEAD`：`281fdd6 → 61d4ac1 (1789710263) → 50fc993 (1789710266)`，两提交相隔 3 秒，与台账「控制器收尾提交」一致 —— 绿灯没有被任何自动化门禁在提交前验证过。
   - 建议：Task 4 起，报告与 Step 5 原始输出必须在 commit 前落到文件，否则不允许提交。

### Minor
2. **计划 966 的两个钳制分支零覆盖（测试空转，计划原文自带）**
   - `tests/magic_level_test.gd:34`：MYTH 满修正 = 0.0075 + 6×0.15 = 0.9075 < 0.95，`clampf` 上界从未触发；`:35`：LEGEND 空修正 = 0.01 > 0.005，下界从未触发；`:23` 的 0.95 来自 `magic_level.gd:46` 的早退，不是钳制。
   - 建议（两行 `eq`）：`effective_rate(PRE_SCHOOL, {六键各 1.0}) == 0.95`；`effective_rate(MYTH, {}, -0.10) == 0.005`。
3. **「十级十档全覆盖」不成立 + 两处宽泛断言**
   - `tests/magic_level_test.gd:8` 只测 `LABELS.size()==10`；`LABELS` 内容仅 3 项；`BANDS` 精确相等仅 5 档，**EXPERT/LEGEND/MYTH/SQUIB 四档数值从未被断言**；`:22` 用 `is_true(BANDS[MASTER].y <= 0.02)` 代替精确相等，失败时无法定位。
   - Money 侧：负值只断言 `total_knuts()`（`:28`），负值 `parts()`/`formatted()`/`to_dict` 往返与 `subtract` 对接收者的值语义未断言。
   - 均为计划 Step 1 原文，实现者照抄无偏差，属计划级补测建议。
4. **章节引用错误（计划 790 = `tests/money_test.gd:28`）**
   - 消息 `"允许负债（第十九章：家族破产）"`，但正典 `:227` 的第十九章是协会体系，与破产无关；正典支撑在 `:46`（第三章：古灵阁贷款使纯血家族破产）、`:451`（第三十八章）、`:575`（第五十三章：继承含债务）。仅文案。
5. **新注释与计划 Task 4 文本自相矛盾（计划 640 新增句）**
   - 新注释称 `4930`「恰好够买一根 7–10 加隆的魔杖」（与正典 `:223` 一致，注释正确）；但计划 Task 4 测试（计划 1060-1061）用 `subtract(1加隆)` 却标注 `"买魔杖后剩 9 加隆"`。同一份计划里魔杖价一处 7–10 加隆、一处 1 加隆，**必须在 Task 4 落地前统一**。
6. **`BANDS[0]=(1.00,1.00)` 与 `effective_rate(SQUIB)=0.95` 不一致（计划 923 vs 961-963）**
   - 结果为哑炮 5% 施法成功率，与正典 `:127`「哑炮…无法施展咒语」的严格读法冲突；Task 7 判定走 `effective_rate`（计划 2735），计划 3570 又对 SQUIB 面板特判。建议人类裁定：改 `BANDS[0]` 为 `(0.95,0.95)`，或 `effective_rate` 对 SQUIB 返回 1.0/直接拒绝施法。

## A–F 逐条裁定

- **A. 计划文本被改 —— 改后期望唯一正确 ✅，注释与正典一致 ✅，无夹带数值改动 ✅。** 510 = 493+17 → `[1,1,0]`、511 → `[1,1,1]` 是正典 `:221` 的唯一规范化解；原文本内部自相矛盾（`[1,0,17]` 与同计划 772 的 `17→[0,1,0]` 冲突；原 `511→[1,1,0]` 合计仅 510 纳特，与计划 794-795 的 `{3,2,5}`（1518）总量守恒冲突），故不存在能同时通过原两条断言的合法实现。注释侧：`4930 = 10加隆`，计划 644 的数值本身未被改动（`data/*.json` 零变更，11 项 JSON 一字未动，全仓再无 `4969`），与计划 1055/1058、1940 三处互相印证；旧注释 `4969` 会格式化为 `"10加隆 2西可 5纳特"`，与三处全冲突，属纯口径笔误。新增 4 个 `.uid` 与 Task 1 既有约定一致，非越权。
- **B. 控制器的 2 条断言改动 —— 与正典一致 ✅，签核：修正测试以匹配正确实现，未掩盖缺陷。** 红→绿只动这两行（另 13 条逐条数过）；半分数语义被同文件独立断言钉住（`:10` 17→`[0,1,0]`、`:20` 7加隆→`[7,0,0]`、`:32` `{3,2,5}`、`:33` 往返总量相等）；任何让原断言通过的实现都必须放弃 17 纳特进位或丢弃 1 纳特，故无「另一种合法实现」。
- **C. `MagicLevel` 逐字符合计划 ✅（无数值/名称/顺序偏差）。** Tier 十级顺序、`LABELS` 10 项、`BANDS` 前六档数值、`MODIFIER_KEYS` 六键名称与顺序、`MODIFIER_WEIGHT := 0.15`、`label_of` 越界 `"未知"`、`index_of_label` 的 `find`→-1、`base_rate` 取中点、`effective_rate` 公式与 `clampf(0.005, 0.95)`、SQUIB 早退忽略 modifiers/aptitude_delta —— 逐项对上计划与正典（`:278-288`、`:290`、`:294`）。仅两点非偏差：`BANDS[0]` 与早退不一致（发现 6）、大师级上界 0.02 对正典 `:288`「低于2%」只算擦边。
- **D. 覆盖 —— 计划要求的断言一条不缺（15+22 与计划逐字一致），但「十级十档全覆盖」不成立。** 见发现 2、3；缺口全部来自计划 Step 1 原文。
- **E. `tests/run_tests.gd` —— 无越权 ✅，位置与 quit 保证无冲突 ✅。** 仅 hunk `@@ -4,6 +4,8 @@`（+2 行），插在 registry 之后（= 计划 Step 2 的 `sed` 语义），`SUITES` 共 4 项，无其它 hunk；`_run_suite` 未改，Task 1 修复后的保证对新增项继续成立；红态走常规计数路径不会挂死。
- **F. 流程合规 —— 确认不合规（见 Important #1）。** 无 `task-3-report.md`，未跑 Step 5；reflog 显示控制器统一收尾（计划修正 61d4ac1 → 代码 50fc993，顺序正确）。严重度：P1（已关闭，不阻塞本次合并）；报告缺失 P2。

## 残余风险 / 未覆盖

- **负值显示未定义**：`Money.from_knuts(-50).parts()` → `[0,-2,-16]`，`formatted()` = `"0加隆 -2西可 -16纳特"`；计划未规定负值显示。第六十二章面板（正典 `:641`）在债务场景会显示该形态，Task 9 前建议裁定。
- **哑炮 5% 成功率**：见发现 6；`base_rate(SQUIB)=1.0` 另超出 `effective_rate` 文档化的 `[0.005,0.95]` 值域，属潜在陷阱。
- **大师级上界**：`0.02` 对上「低于2%」是开/闭区间歧义，测试用 `<=` 掩盖。
- **reviewer 无法复现**：只读无 shell，不能独立跑 `bash tools/test.sh`/`git`；断言条数已逐条复核（15、22 对得上）；Step 2（红）无日志留存；「提交后工作区干净」仅凭控制器陈述。
- **运行时假设**：`[1,0,0]` 与 `Array[int]` 的深比较、精确 `0.95` 依赖 Godot Array 深比较语义；控制器观测 0 失败说明成立，冷缓存/其它构建未覆盖。
- 未处理的计划级缺口（发现 3/5/6）会以同型形态复制到 Task 5/7/9 的测试里。
