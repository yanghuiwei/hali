# 统一换行符为 LF，避免 Windows/CI 之间的无谓 diff
* text=auto eol=lf

# 二进制资源不参与换行符转换（音频 / 字体 / 贴图 / 切片源文件 / 压缩包）
*.png binary
*.jpg binary
*.jpeg binary
*.ogg binary
*.mp3 binary
*.wav binary
*.ttf binary
*.otf binary
*.psd binary
*.zip binary
*.exe binary
# SVG 是 XML 文本，但 game-icons.net 出品为单行压缩形式，按二进制处理可避免无意义 diff
*.svg binary
